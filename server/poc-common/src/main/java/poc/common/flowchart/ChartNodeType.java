package poc.common.flowchart;

public enum ChartNodeType {
    ATOMIC,
    COMPOSITE,
    GOTO,
    PERFORM,
    IF_BRANCH,
    DUMMY,
    CONTROL_FLOW,
    PARAGRAPH, SECTION, CONDITION_CLAUSE, SENTENCE, IF_YES, IF_NO, DIALECT
}
